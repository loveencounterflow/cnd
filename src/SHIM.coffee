


#-----------------------------------------------------------------------------------------------------------
@shim = ( keys... ) ->
  throw new Error "keys not yet implemented; use `shim()` instead" if keys.length > 0



